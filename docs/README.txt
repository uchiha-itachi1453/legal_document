Optional files in this folder can be listed in public/docs-manifest.json and offered in the app
as extra downloads. Only the file name (basename) is used from each entry’s path; keep files
flat under docs/ (no subfolders required).
